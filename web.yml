---
 hosts: devserver
  become: yes
  ignore_errors: yes
  tasks:
    -name: Install package
    packages:
      name: "{{ item }}"
      state: present
    with_items:
        - unzip
        - wget
        - htppd
      -name: download
      shell:

